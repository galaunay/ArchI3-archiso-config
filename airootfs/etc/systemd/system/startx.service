[Unit]
Description=automatic startx for user %u
After=graphical.target systemd-user-sessions.service

[Service]
User=%u
WorkingDirectory=%h
PAMName=login
Type=simple
ExecStart=/bin/bash -l -c startx

[Install]
WantedBy=graphical.target
